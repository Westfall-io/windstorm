[tool.poetry]
name = "sysml-windstorm"
version = "0.5.1"
description = "Tool to support SysMLv2 model templating for any text based file."
authors = ["Christopher Cox"]
license = "MIT"
readme = "README.md"
packages = [{ include = "windstorm", from = "src" }]

[tool.poetry.dependencies]
python = "^3.9"
fire = "0.6.0"
jinja2 = "3.1.4"
requests = "2.32.3"

[tool.poetry.scripts]
windstorm = "windstorm.windstorm:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["src/windstorm"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

#[tool.semantic_release]
#branch = "dev0.1.x"
#version_toml = ["pyproject.toml:tool.poetry.version",
#"pyproject.toml:project.version",]
#version_variable = "src/windstorm/__init__.py:version"
#version_source = "tag"
#commit_version_number = true # required for version_source = "tag"
#tag_commit = true
#commit_parser = "semantic_release.history.emoji_parser"
#patch_without_tag = true
#build_command = "pip install poetry && poetry build"

[tool.coverage.run]
omit = [
    "src/api/__init__.py",
    "src/main.py"
]

[tool.semantic_release]
version_variable = [
    "src/windstorm/__init__.py:version",
]
version_toml=[
    "pyproject.toml:tool.poetry.version",
    "pyproject.toml:project.version",
]
commit_parser = "emoji"
build_command = "pip install poetry && poetry build"
commit_message = ":bookmark:{version}\n\nAutomatically generated by python-semantic-release"

[tool.semantic_release.changelog]
exclude_commit_patterns = [":robot:",":twisted_rightwards_arrows:",
":poop:", "^[^:].*"]

[tool.semantic_release.commit_parser_options]
major_tags = [":boom:"]
minor_tags = [
    ":sparkles:",
    ":children_crossing:",
    ":lipstick:",
    ":iphone:",
    ":egg:",
    ":chart_with_upwards_trend:",
]
patch_tags = [
    ":ambulance:",
    ":lock:",
    ":bug:",
    ":zap:",
    ":goal_net:",
    ":alien:",
    ":wheelchair:",
    ":speech_balloon:",
    ":mag:",
    ":apple:",
    ":penguin:",
    ":checkered_flag:",
    ":robot:",
    ":green_apple:",
    ":memo:",
    ":heavy_plus_sign:",
    ":construction:",
    ":white_check_mark:",
]

[tool.semantic_release.branches.main]
match = "(main|master)"
prerelease = false
build_command = "pip install poetry && poetry build"

[tool.semantic_release.branches."dev0.1.x"]
match = "dev0.1.x"
prerelease = true
prerelease_token = "rc"
build_command = "pip install poetry && poetry build"

[project.urls]
"Homepage" = "https://github.com/Westfall-io/windstorm"
"Bug Tracker" = "https://github.com/Westfall-io/windstorm/issues"

[project]
name = "sysml-windstorm"
version = "0.5.1"
authors = [
  { name="Christopher Cox", email="chris.cox@westfall.io" },
]
description = "SysML v2.0 Parser"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Development Status :: 1 - Planning",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.scripts]
windstorm = "windstorm.windstorm:main"
